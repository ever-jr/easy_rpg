import 'package:flutter/material.dart';

class ColorPalette {
  const ColorPalette._();

  static const dark = Color(0xFF212026);
  static const lessDark = Color(0xFF352D40);
  static const light = Color(0xFF9081A6);
}