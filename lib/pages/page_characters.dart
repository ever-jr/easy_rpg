import 'package:flutter/material.dart';

final class PageCharacters extends StatelessWidget {
  const PageCharacters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Characters'),
    );
  }
}
