import 'package:flutter/material.dart';

class PageCampaigns extends StatelessWidget {
  const PageCampaigns({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Campaigns'),
    );
  }
}
